# v0.3.0 发布验证记录

## 范围与环境

- 日期：2026-07-19
- 平台：macOS arm64
- 运行时：Deno 2.7.1、Python 3
- 范围：Issue #86 Source Turn 阶段一、#87 Unified Event/Source Turn、#88
  Turn-first Task、#90 私有 Ground Truth 门禁和 #89 `v0.3.0` 发布
- 私有数据边界：不提交真实日志、内容、路径、Session ID、个人报告、逐 case 标签或
  私有 scorer 输出

## Source Turn 与 Ground Truth 门禁

| 检查 | 实测结果 |
| --- | --- |
| Unified Event v2 | 稳定 Event/Turn ID、parser version、digest、父子引用和受限 `sourceRef` 已覆盖 |
| Source Turn | 原生 `turn_context` 优先；旧日志回退边界标记 `inferred | partial` |
| Tool 配对 | 原生 `call_id` 配对；成功、失败与 unmatched diagnostics 可观察 |
| Task provenance | Task、Outcome、Verification、Evidence 引用 Source Turn/Event |
| Task boundary agreement | 23/26，88.5%，门槛 >= 80% |
| Project agreement | 19/23，82.6%，门槛 >= 80% |
| Multi-goal switch recall | 3/3，100%，硬门槛 100% |
| Explicit Verification recall | 18/18，100%，门槛 >= 80% |
| Cross-session precision | 7/7，100%，门槛 >= 80% |
| Cross-session provenance | 7/7 Task 的 Event/Turn/本地受限来源引用完整 |
| 最终关系投影 | 四天报告的 Task 自关系为 0 |

私有 scorer 从冻结的只读源重新扫描四个日报窗口并复算上述结果。仓库内只记录聚合指标，
完整口径见 `docs/TESTS/real-task-ground-truth-eval-v0.2.md`。

## 发布候选门禁

| 检查 | 实测结果 |
| --- | --- |
| `deno task verify` | fmt、lint、typecheck 与 197/197 Deno tests 通过 |
| scoring baseline | Python unit 16/16；100-case conformance quality gate 通过 |
| progressive analysis | Python unit 8/8；share-safe conformance quality gate 通过 |
| privacy、shell syntax、diff whitespace | 全部通过 |
| 双架构编译 | arm64 与 x86_64 编译通过；本机 arm64 返回 `0.3.0` |
| 离线安装 smoke | checksum 安装成功；`version`、`doctor`、合成 `report --no-ai` 通过 |
| 报告迁移契约 | 新报告 provenance 为 parser v2，Task 至少引用一个 Source Turn |

复现命令：

```sh
deno task verify
python3 -m unittest tests/eval/test_scoring_baseline.py -v
python3 scripts/eval_scoring_baseline.py \
  --rubric tests/eval/scoring-baseline/rubric.v1.json \
  --cases tests/eval/scoring-baseline/cases.v1.jsonl \
  --predictions tests/eval/scoring-baseline/predictions.conformance.jsonl
python3 -m unittest tests/eval/test_progressive_analysis.py -v
python3 scripts/eval_progressive_analysis.py \
  --gold tests/eval/progressive-analysis/gold.synthetic.v1.jsonl \
  --predictions tests/eval/progressive-analysis/predictions.conformance.v1.jsonl \
  --share-safe
sh scripts/privacy_check.sh
sh -n scripts/install.sh scripts/uninstall.sh scripts/privacy_check.sh
git diff --check
```

此外编译 `aarch64-apple-darwin` 与 `x86_64-apple-darwin` 两个目标，并用本机 arm64
二进制执行 `version`、`doctor`、`report --no-ai` 和离线安装器覆盖安装 smoke。

## 发布后验收

Release 发布后记录 PR、required checks、tag workflow、两个二进制、checksums、provenance
attestation、覆盖安装后的 `aci version`、`aci doctor` 和 `aci report --no-ai`。任一项失败
都不关闭 #89 或父 Issue #86。
