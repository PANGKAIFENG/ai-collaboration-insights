# v0.2.2 发布验证记录

## 范围与环境

- 日期：2026-07-17
- 平台：macOS arm64
- 运行时：Deno 2.7.1、Python 3.12、codex-cli 0.131.0
- 范围：Issue #78 自定义 provider 路由、核心任务分批、部分失败保留，以及
  `v0.2.2` 安装发布
- 私有数据边界：不提交真实日志、内容、路径、逐条诊断或个人报告；真实 provider
  只用于本机 synthetic 输入验收，不修改持久 consent 或凭据

## Issue 级验证

| 检查 | 实测结果 |
| --- | --- |
| 根因复现 | `--ignore-user-config` 丢失自定义 provider 路由后反复重连并回退，synthetic 单任务在 126.75 秒后超时 |
| 路由修复 | 只转发白名单 provider 路由并使用低推理强度，同一 synthetic 单任务在 20.88 秒完成 |
| 大任务量 | 9 个 synthetic 任务分为 `4 / 4 / 1` 三批；9/9 分析、9 条洞察、3 条建议，总耗时 113.8 秒 |
| 隔离失败 | 单批超时返回 `partial`，保留成功批次和覆盖率；全部批次失败才返回 `degraded` |
| 隐私边界 | 不读 `auth.json`；headers、query params、MCP、hooks、plugins 和危险 provider URL 不进入 argv |
| GitHub CI | [PR #79](https://github.com/PANGKAIFENG/ai-collaboration-insights/pull/79) 的 quality、arm64、x86_64 全部通过 |

## 发布候选门禁

| 检查 | 实测结果 |
| --- | --- |
| `deno task verify` | fmt、lint、typecheck 与 102/102 测试通过 |
| scoring baseline | unit 16/16；conformance quality gate 通过 |
| progressive analysis | unit 8/8；conformance quality gate 通过 |
| privacy、shell syntax、diff whitespace | 全部通过 |
| 双架构编译 | arm64 与 x86_64 编译通过；本机 arm64 二进制返回 `0.2.2` |
| 版本一致性 | CLI、doctor、安装 smoke 与 CI 将统一为 `0.2.2`；Release workflow 校验 tag 一致性 |

复现命令：

```sh
deno task verify
python3 -m unittest tests/eval/test_scoring_baseline.py -v
python3 scripts/eval_scoring_baseline.py \
  --rubric tests/eval/scoring-baseline/rubric.v1.json \
  --cases tests/eval/scoring-baseline/cases.v1.jsonl \
  --predictions tests/eval/scoring-baseline/predictions.conformance.jsonl
python3 -m unittest tests/eval/test_progressive_analysis.py -v
python3 scripts/eval_progressive_analysis.py \
  --gold tests/eval/progressive-analysis/gold.synthetic.v1.jsonl \
  --predictions tests/eval/progressive-analysis/predictions.conformance.v1.jsonl \
  --share-safe
sh scripts/privacy_check.sh
sh -n scripts/install.sh scripts/uninstall.sh scripts/privacy_check.sh
git diff --check
```

## 发布后验收

发布完成后回填 Release workflow、公开 assets、checksums、双架构 provenance、官方
安装脚本覆盖安装、本机版本、doctor、schedule、报告与 consent 数据保留结果。
