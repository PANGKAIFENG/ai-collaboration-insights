# v0.2.1 发布验证记录

## 范围与环境

- 日期：2026-07-16
- 平台：macOS arm64
- 运行时：Deno 2.7.1、Python 3.12
- 范围：Issue #68 路径脱敏、#67 日报窗口 Token 增量、#69 注入上下文归一化，以及 `v0.2.1` 安装发布
- 私有数据边界：不提交真实日志、内容、路径、逐条诊断或个人报告；真实日志仅用于本机只读验收

## Issue 级验证

| Issue | 核心回归证据 | GitHub required checks |
| --- | --- | --- |
| #68 | 确定性任务名和成果中的 macOS/Linux 路径均被脱敏 | PR #73：quality、arm64、x86_64 通过 |
| #67 | 窗口内调用增量求和；相邻与非相邻重复累计快照均去重；无增量时保留累计峰值回退 | PR #74：quality、arm64、x86_64 通过 |
| #69 | 大段或分离 content item 的真实请求在截断前保留；纯 Files/Applications/Automation 上下文被过滤 | PR #75：quality、arm64、x86_64 通过 |

## 发布候选门禁

| 检查 | 实测结果 |
| --- | --- |
| `deno task verify` | fmt、lint、typecheck 与完整测试通过 |
| scoring baseline | unit 16/16；conformance quality gate 通过 |
| progressive analysis | unit 8/8；conformance quality gate 通过 |
| privacy、shell syntax、diff whitespace | 全部通过 |
| 版本一致性 | CLI、doctor 与安装 smoke 为 `0.2.1`；Release workflow 校验 tag 一致性 |

复现命令：

```sh
deno task verify
python3 -m unittest tests/eval/test_scoring_baseline.py -v
python3 scripts/eval_scoring_baseline.py \
  --rubric tests/eval/scoring-baseline/rubric.v1.json \
  --cases tests/eval/scoring-baseline/cases.v1.jsonl \
  --predictions tests/eval/scoring-baseline/predictions.conformance.jsonl
python3 -m unittest tests/eval/test_progressive_analysis.py -v
python3 scripts/eval_progressive_analysis.py \
  --gold tests/eval/progressive-analysis/gold.synthetic.v1.jsonl \
  --predictions tests/eval/progressive-analysis/predictions.conformance.v1.jsonl \
  --share-safe
sh scripts/privacy_check.sh
sh -n scripts/install.sh scripts/uninstall.sh scripts/privacy_check.sh
git diff --check
```

## 发布后验收

| 检查 | 实测结果 |
| --- | --- |
| Release PR | [#76](https://github.com/PANGKAIFENG/ai-collaboration-insights/pull/76) 合并为 `d8a2fcb`；PR 与合并后 `main` 的 quality、arm64、x86_64 CI 均通过 |
| Release workflow | [run 29510748410](https://github.com/PANGKAIFENG/ai-collaboration-insights/actions/runs/29510748410) 的 tag 版本校验、双架构构建、provenance 与发布任务全部通过 |
| 公开 Release | [v0.2.1](https://github.com/PANGKAIFENG/ai-collaboration-insights/releases/tag/v0.2.1) 非 draft / prerelease，包含两个 macOS 二进制、`checksums.txt`、`install.sh`、`uninstall.sh` |
| 完整性与来源 | arm64 官方 asset 的 SHA-256 与 Release digest 一致；两个二进制各有一个 `https://slsa.dev/provenance/v1` attestation，subject 名称和 digest 与 Release assets 一致 |
| 覆盖安装 | 使用 Release 官方 asset、`checksums.txt` 和官方 `install.sh` 覆盖安装；已安装二进制为 `0.2.1`，SHA-256 与 arm64 Release asset 一致，`gh attestation verify` 通过 |
| 本地运行状态 | `aci doctor` 的 Codex sessions、数据目录和 schedule 均 ready / loaded；LaunchAgent 在覆盖安装后 RunAtLoad 成功退出，仍为每天 19:00 |
| 真实日报 | 本机只读重算后，JSON 与 HTML 的绝对路径命中为 0，任务展示字段路径命中为 0，注入上下文任务标题为 0；Token 指标采用窗口内调用增量并发生预期口径修正 |
| 数据保留 | 覆盖安装前后既有报告数量不变；本机当时没有 consent 文件，因此 consent 保留仅由安装器不写应用数据的实现边界和 install lifecycle 自动测试覆盖 |

Release workflow 仍有 GitHub Actions Node.js 20 deprecation warning，不影响本次构建或发布，继续由 Issue #66 跟踪。
