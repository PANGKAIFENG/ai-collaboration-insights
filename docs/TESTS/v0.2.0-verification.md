# v0.2.0 发布验证记录

## 范围与环境

- 日期：2026-07-15
- 平台：macOS arm64
- 运行时：Deno 2.7.1、Python 3
- 范围：Issue #57 progressive-analysis eval、任务 orphan 修复、版本升级、安装和静态报告生命周期
- 私有数据边界：只记录 share-safe 聚合指标，不提交真实日志、内容、路径、case ID、逐条标注或报告

## 评估门禁

| 门禁 | 结果 |
| --- | --- |
| 公开 progressive-analysis unit eval | 8/8 通过 |
| 公开 conformance | 通过 |
| threshold failure 行为 | overall F1 0.9444、`system_scaffolding=0.6667` 时正确返回 exit code 1 |
| 私有任务分组 | 23 个最终任务组；overall F1 1.0；六个 required slice 均为 1.0 |
| 私有语义轮次 | 30 个标注轮次；accuracy 0.60；非阻断缺口记录为 Issue #64 |
| 私有 evidence、coverage、degradation 与 relation diagnostics | 均为 1.0 |
| 私有 quality gate | 通过 |

任务分组主门槛为 overall F1 `>=0.80`，六个 required slice 各 `>=0.70`。语义轮次指标在本版作为独立诊断，不覆盖任务分组质量门槛。

## 发布候选自动化验证

2026-07-15 在发布候选分支完成：

| 检查 | 实测结果 |
| --- | --- |
| `deno task verify` | fmt、lint、typecheck 通过；93/93 tests 通过 |
| scoring baseline unit eval | 16/16 通过 |
| progressive-analysis unit eval | 8/8 通过 |
| 公开 progressive-analysis conformance | exit code 0；quality gate 通过 |
| 私有 progressive-analysis gate | exit code 0；仅输出 share-safe 聚合结果 |
| privacy、shell syntax、diff whitespace | 全部通过 |
| arm64 binary version 与本地安装生命周期 | `0.2.0`；checksum、doctor、report、保留报告重装和显式 purge 通过 |
| 真实静态报告浏览器验收 | 1440px 与 375px 均无横向溢出、越界元素、任务重叠、脚本、远程资源或 console error |

复现命令：

```sh
deno task verify
python3 -m unittest tests/eval/test_scoring_baseline.py -v
python3 -m unittest tests/eval/test_progressive_analysis.py -v
python3 scripts/eval_progressive_analysis.py \
  --gold tests/eval/progressive-analysis/gold.synthetic.v1.jsonl \
  --predictions tests/eval/progressive-analysis/predictions.conformance.v1.jsonl \
  --share-safe
sh scripts/privacy_check.sh
sh -n scripts/install.sh scripts/uninstall.sh scripts/privacy_check.sh
git diff --check
```

一次经授权的 synthetic AI smoke 自然结束为 deterministic degraded，原因为 Codex core analysis timeout；报告仍成功发布。该结果不影响默认 no-AI 主路径和本次任务分组质量门禁，也不被记录为 AI enrichment 成功。

PR required checks、tag Release workflow、build provenance 和公开 Release 安装结果作为 GitHub 侧发布证据，不在仓库中写入令牌、私有路径或个人报告。

## 发布后验收

1. GitHub Release 包含 Apple Silicon / Intel 二进制、`checksums.txt`、安装和卸载脚本。
2. 两个二进制均有 build provenance attestation。
3. 从公开 Release URL 安装后，`aci version` 输出 `0.2.0`。
4. `aci doctor`、`aci schedule status` 和 `aci report --no-ai` 成功。
5. 默认卸载保留报告；从同一 Release 重装后报告仍存在。
6. `--purge-data` 只删除应用派生目录，不触碰 Codex 源日志或登录态。
