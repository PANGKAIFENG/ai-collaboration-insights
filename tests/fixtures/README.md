# Synthetic Fixtures

Store small, synthetic, version-labeled source-log fixtures here. Each fixture set must identify the source tool, simulated version, supported fields, expected normalized output, and privacy review status.

Private or local-only fixtures remain under ignored paths and must never be committed.
