# End-to-End Tests

Store clean-environment workflows that exercise the local service, dashboard, consent, report generation, correction, export, and deletion behavior here.

End-to-end tests must verify localhost-only access and must not contact a real model service unless an explicitly gated test supplies user-owned credentials.
