# Integration Tests

Store tests that verify behavior across adapters, persistence, task reconstruction, scheduling, analysis, scoring, and report generation here.

Every suite should name the user-visible or system-visible contract it protects and use isolated temporary data.
